PORT=3001
NODE_ENV=development

# Database (MySQL + Sequelize) — configurar en etapa posterior
DB_HOST=localhost
DB_PORT=3306
DB_NAME=focusmind
DB_USER=focusmind
DB_PASSWORD=

# JWT — configurar en etapa posterior
JWT_SECRET=
JWT_EXPIRES_IN=7d
